[package]
name = "doomterm_plate"
version = "1.0.0"
edition = "2021"
description = "Pure Doom Term status plate geometry, glyph rendering, state representation and pixel operations"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
